{
  "name": "ig-images",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Instagram 投稿用の画像を日次生成して公開配信する（OpenAI GPT Image で背景生成 + sharp で日本語テキスト合成）。生成は GitHub Actions が行い、API キーは GitHub Secrets に置く。",
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
